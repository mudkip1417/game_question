<%= stylesheet_link_tag 'users', :media => "all" %>


<div class="container">

  <div class="m-5 text-center">

    <h1 class="mb-3 font-weight-normal">Log in</h1>


    <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>

        <%= f.label :email, class: "sr-only" %><br />
        <%= f.email_field :email, autofocus: true, placeholder: "email", autocomplete: "email", class: "form-controll" %>

        <%= f.label :password, class: "sr-only" %><br />
        <%= f.password_field :password, placeholder: "password", autocomplete: "current-password", class: "form-controll" %>

      <% if devise_mapping.rememberable? %>
        <div class="checkbox">
          <%= f.check_box :remember_me %>
          <%= f.label :remember_me %>
        </div>
      <% end %>

      <div class="mx-auto" style="width: 200px; margin-top: 20px;">
        <%= f.submit "Log in",class:"btn btn-lg btn-success btn-block" %>
      </div>
    <% end %>

    <br>
    <br>
    登録がお済みではない方は<%= link_to "こちら", new_registration_path(resource_name) %>から新規登録を行ってください。

  </div>

</div>



<% render "public/shared/links" %>

